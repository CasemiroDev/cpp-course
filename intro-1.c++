#include <iostream> //biblioteca padrão do C++

using namespace std; //comando para evitar o prefixo std:: antes de cada elemento da biblioteca padrão do C++. 

int main()
{
    cout << "Olá, IFCE! Cheguei!";

    return 0;
}